# Use a shared public guest mode

Trace will offer guest exploration through a single shared server-backed Guest identity instead of a browser-only sandbox or per-visitor anonymous accounts. This keeps the experience simple and synced across devices without adding a new identity provider flow, at the cost of making guest categories and time history public and editable by all guests.
